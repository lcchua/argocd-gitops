apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flask-lcchua.fullname" . }}-test-connection"
  labels:
    {{- include "flask-lcchua.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "flask-lcchua.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
